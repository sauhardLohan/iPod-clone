const CoverFlow = (props) => {
  // Coverflow section to show it's heading
  return (
    <div
      style={{
        alignItems: "center",
        display: "flex",
        justifyItems: "center",
        width: "100%"
      }}
    >
      <p
        style={{
          textAlign: "center",
          width: "100%",
          fontSize: 20,
          fontWeight: 700
        }}
      >
        Cover Flow
      </p>
    </div>
  );
};
export default CoverFlow;
